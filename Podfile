# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PTNPurse' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  pod 'SVProgressHUD', '~> 1.1.3'
  pod 'YYText', '~> 1.0.7’
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'SDWebImage'
  pod "UXPasscodeField"
  use_frameworks!

  # Pods for PTNPurse

  target 'PTNPurseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PTNPurseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
